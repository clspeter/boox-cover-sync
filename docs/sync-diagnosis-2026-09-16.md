# 2026-09-16 封面落後數本：尚未完成修復

## 後續發現：BOOX 4.2 的背景時間上限

- 23:50 左右，依社群第一手記錄 <https://github.com/Kisuke-CZE/Palma_2_Pro-tips#enabling-app-to-run-in-background> 查實機。App 圖示長按 → 優化 → 其它 →「允許後台持續運行」顯示 **5分鐘**；目前 E InkWise 設定檔為「個性化」。
- 將此 App 改為「持續運行」，確認後關閉並重新開啟選單，仍顯示「持續運行」。未調整其他 App。
- 這個上限不同於凍結、Doze 白名單與 app-op，解釋了先前檢查的缺口。但設定相關性尚不是 force-stop 呼叫堆疊的證據，仍須實測服務跨越原時間上限。
- 23:51 重新啟用本 App 無障礙服務、保留其他服務後，回到主畫面開始有時間上限的只讀追蹤。尚未將此結果宣稱為整體修復。
- 23:51:46 起觀察 540 秒，共 51 次採樣：`packageStopped=false`、`accessibilityEnabled=true`、`serviceBound=true`、`serviceListedCrashed=false` 全程未變；跨越原本 5 分鐘上限。這是單次背景存活驗證，不代表已證實所有 force-stop 原因。

## 2026-09-17：手動開 App 同步備援

- 新增冷啟動、從桌面返回時單次查詢並同步最新閱讀紀錄，亦提供「同步最新閱讀封面」按鈕。從 App 自己開啟的選檔器／設定頁返回不觸發；configuration recreation 不重複查詢。與背景開關及無障礙服務無關。
- 80 個單元測試、lint、APK build 通過；以 `adb install -r` 覆蓋安裝成功。
- 暫時移除本 App 無障礙設定（保留其他服務），force-stop 後冷啟動、HOME 後熱返回都驗證輸出檔重新寫入、fingerprint 與 Metadata 最新候選一致。同一本仍可重送。
- 休眠畫面與剛輸出圖片於記憶體內縮成 96×128 灰階比較，平均差為 0；沒有保存私人書籍識別或封面。BOOX screencap 在 PNG 前輸出診斷文字，第一次驗收腳本解析失敗，改讀有效 PNG 後通過，這不是同步失敗。
- 測試後已恢復原有本 App 無障礙服務，確認正常連接，並確認覆蓋更新後「持續運行」仍保存。未新增右下角無障礙按鈕；它仍依賴服務存活。未進行完全關機測試。

## 實機觀測

- 使用者回報休眠封面為前面數本。休眠截圖與 App 最後送出的圖片縮小至相同灰階尺寸比較，平均絕對差為 0；當時已同步的 HMAC 在 Metadata 候選排序中為第 4 筆。未保存或輸出真實書名、URI、HMAC 或書籍內容至本文件。
- 喚醒後曾追上 Metadata 最新候選，但這只能證明同步到最近存取紀錄，不能單憑這點確認是正在閱讀的書。
- 重裝後 BOOX 紀錄 `isAutoFreeze=true`，套件最初為 disabled-user。`pm enable` 與 Doze 白名單不會修改 BOOX 自動凍結設定。後續服務被系統 force-stop，無障礙設定為 null。
- 在 BOOX「應用」頁長按 App **圖示**，選「解凍」後，BOOX 紀錄 `isAutoFreeze=false`。重新啟用無障礙服務後，合成 EPUB A 約 5 秒、B 約 8 秒完成同步；兩次休眠畫面均與相應封面一致，服務當時仍正常連接。
- B 首次休眠截圖是在畫面尚未切換完成時取得，不能用作最終畫面；穩定後重測差值為 0。
- 較晚仍發生另一次 force-stop：Android exit-info 為 reason 10 (`USER REQUESTED`)，description 指向 system_server。使用者確認沒有清理背景或關閉無障礙；此分類不能用來推定是使用者操作。
- 停止的時間與 HOME／uiautomator 診斷相近，但隔離重跑前景及背景的 uiautomator 查詢均未再次停止服務，因此「診斷工具造成」尚未證實。
- 嘗試透過公開 ReaderHomeActivity 返回最新非測試 EPUB，20 秒內未確認切換成功；Metadata 最新候選仍是測試 EPUB。因此一般書籍的最後一輪實機驗收尚未完成，不能解讀成一般 EPUB 解析失敗。

## 已完成、但不足以宣稱整體修好

- 排程保留第一次事件的查詢期限，避免持續事件一直推遲工作。
- 同步成功後仍用完剩餘有限查詢，避免暫時舊候選成功後就停止。
- 71 個 tests、lint、APK build 通過；已用相同簽章重裝並確認裝置 APK hash 符合建置產物。
- 已還原 App 設定、儲存與定位授權、Doze 白名單，並從 BOOX 選單解除凍結。
- 暫時 pipeline 診斷 Log 在編譯部署前已全部移除；沒有帶診斷 Log 的 APK 留在裝置。

## 尚待驗證的假設

1. **解除凍結與省電白名單足以保證服務存活**：已被後續停止現象否定；尚需找出實際呼叫 force-stop 的 BOOX／系統路徑。
2. **Metadata 最新 EPUB 就是目前閱讀文件**：對合成 A/B 測試成立，對使用者原本書籍尚未完成獨立確認。
3. **畫面結構診斷會停止本服務**：只有時間相關性，隔離測試未重現。

依使用者「連續三次修復失敗後停止」的要求，停止進一步修正，不再以延長 retry、重裝或反覆重新啟用作為完成修復的證據。下一次工作應先定位 force-stop 的實際來源；每輪以服務持續連接、目前書籍身份、實際休眠畫面三者同時通過為驗收條件。

## 後續：使用者提出喚醒補查與追蹤方式

- 新增 `scripts/boox-watch-service.ps1`，在限定時間內純讀取螢幕、套件、無障礙與最近退出狀態，僅在變化時輸出匿名 JSON。實跑確認可辨識 `stopped=true`、無障礙關閉與服務未連接。
- 再次觀測到 23:08:06.869 系統強制停止，與 23:00:06.868 相隔約 8 分鐘；只有兩次時間點，尚不能證明存在週期性工作。Android 的背景執行 app-op 為 allow，套件仍在 Doze 白名單，standby bucket 為 exempted。
- 在 `stopped=true` 狀態做休眠／喚醒，套件與服務未自行恢復。因此不得將「喚醒補查」宣稱為 force-stop 後的自啟動。
- 依使用者的新方向，`onScreenOn()` 改為發起既有的有限查詢循環。新增測試先由 13 tests／1 failure 轉為 17 tests／0 failures，涵蓋無視窗事件的喚醒、事件合併、停用與熄屏取消。強制停止的最初發起元件仍待查明。
- 新版完整驗證為 76 tests、0 failures、0 errors，lint 與 APK build 成功；23:17 以 `adb install -r` 覆蓋安裝成功。重新啟用服務後，23:18 的休眠／喚醒及 20 秒追蹤均維持服務連接，未出現新的非安裝 force-stop。
- 此輪合成 A 的 Metadata 約 29 秒才變成最新，33.6 秒時仍未同步；接續喚醒後再核對，最後同步身分已等於 Metadata 第 1 筆。這輪夾帶閱讀器視窗事件，不能獨立證明只有喚醒事件觸發同步；無視窗事件的行為由單元測試驗證。未重新驗收休眠封面圖，亦未證明長時間存活。
